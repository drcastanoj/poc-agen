{
  "name": "poc-agent-lambda-api",
  "private": true,
  "version": "0.1.0",
  "description": "API Gateway handler for the coder agent console — enqueues runs, reads run state and logs. See docs/lambda-migration.md.",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
